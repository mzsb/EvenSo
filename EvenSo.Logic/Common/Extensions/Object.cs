using EvenSo.PropertyTrees;
using System.Xml.Linq;

namespace EvenSo
{
    public static class Objects
    {
        
        //public static IEnumerable<PropertyNode> ToNodes(this object rootValue)
        //{
        //    var nodes = new Stack<PropertyNode>(new[] { new PropertyNode(rootValue) });
        //    while(nodes.TryPop(out var node))
        //    {
        //        yield return node;
        //        foreach (var childNode in node.Children)
        //        {
        //            nodes.Push(childNode);
        //        }
        //    }
        //}
    }
}
